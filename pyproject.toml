[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
target-version = "py312"
line-length = 120
exclude = [
  "data",
  "runtime",
  "uploads",
  "fonts",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
